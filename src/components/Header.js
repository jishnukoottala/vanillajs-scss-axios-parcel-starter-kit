const Header = () => {
  const template = `
    
    <header>
    <h1>Header</h1>
    <p>Boilerplate for a simple js application with scss - component style </p>
    <header>
    `;

  return template;
};

export default Header;
